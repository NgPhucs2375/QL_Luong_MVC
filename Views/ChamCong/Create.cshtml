@{
    ViewBag.Title = "Thêm mới chấm công";
}

<div class="d-sm-flex align-items-center justify-content-between mb-4">
    <h1 class="h3 mb-0 text-gray-800">
        <i class="fas fa-plus-circle text-success mr-2"></i>Thêm mới chấm công
    </h1>
</div>

<div class="row justify-content-center">
    <div class="col-lg-6 col-md-8">

        @* Hiển thị thông báo lỗi nếu có *@
        @if (ViewBag.ErrorMessage != null)
        {
            <div class="alert alert-danger alert-dismissible fade show shadow-sm" role="alert">
                <i class="fas fa-exclamation-circle mr-2"></i> @ViewBag.ErrorMessage
                <button type="button" class="close" data-dismiss="alert" aria-label="Close">
                    <span aria-hidden="true">&times;</span>
                </button>
            </div>
        }

        <div class="card card-plus shadow mb-4 border-top-success">
            <div class="card-header py-3 d-flex flex-row align-items-center justify-content-between bg-white">
                <h6 class="m-0 font-weight-bold text-success">
                    <i class="fas fa-calendar-plus"></i> Tạo bản ghi chấm công mới
                </h6>
                <span class="badge badge-success">Thêm mới</span>
            </div>
            <div class="card-body">
                @using (Html.BeginForm("Create", "ChamCong", FormMethod.Post, new { @class = "user" }))
                {
                    @Html.AntiForgeryToken()

                    @* 1. Chọn Nhân viên *@
                    <div class="form-group">
                        <label class="small font-weight-bold text-gray-700">
                            Nhân viên <span class="text-danger">*</span>
                        </label>
                        <div class="input-group">
                            <div class="input-group-prepend">
                                <span class="input-group-text bg-light border-0"><i class="fas fa-user"></i></span>
                            </div>
                            <select name="IDNhanVien_ChamCong" class="form-control" required>
                                <option value="">-- Chọn nhân viên --</option>
                                @if (ViewBag.DanhSachNhanVien != null)
                                {
                                    foreach (var item in ViewBag.DanhSachNhanVien)
                                    {
                                        <option value="@item.Value">@item.Text</option>
                                    }
                                }
                            </select>
                        </div>
                        @Html.ValidationMessage("IDNhanVien_ChamCong", "", new { @class = "text-danger small" })
                    </div>

                    @* 2. Ngày chấm công *@
                    <div class="form-group">
                        <label class="small font-weight-bold text-gray-700">
                            Ngày chấm công <span class="text-danger">*</span>
                        </label>
                        <div class="input-group">
                            <div class="input-group-prepend">
                                <span class="input-group-text bg-light border-0"><i class="far fa-calendar-alt"></i></span>
                            </div>
                            <input type="date" name="Day_ChamCong" class="form-control form-control-user" value="@ViewBag.NgayMacDinh" required />
                        </div>
                        @Html.ValidationMessage("Day_ChamCong", "", new { @class = "text-danger small" })
                        <small class="form-text text-muted">Chọn ngày cần chấm công</small>
                    </div>

                    <div class="row">
                        @* 3. Ngày công *@
                        <div class="col-md-6">
                            <div class="form-group">
                                <label class="small font-weight-bold text-gray-700">
                                    Ngày công <span class="text-danger">*</span>
                                </label>
                                <input type="number" name="DayCong_ChamCong" class="form-control form-control-user font-weight-bold text-primary" value="1" step="0.1" min="0" max="1" required />
                                @Html.ValidationMessage("DayCong_ChamCong", "", new { @class = "text-danger small" })
                                <small class="form-text text-muted">Nhập từ 0 đến 1 (VD: 0.5, 1.0)</small>
                            </div>
                        </div>

                        @* 4. Giờ tăng ca *@
                        <div class="col-md-6">
                            <div class="form-group">
                                <label class="small font-weight-bold text-gray-700">Giờ tăng ca</label>
                                <input type="number" name="GioTangCa" class="form-control form-control-user" value="0" step="0.5" min="0" />
                                @Html.ValidationMessage("GioTangCa", "", new { @class = "text-danger small" })
                                <small class="form-text text-muted">Số giờ làm thêm (VD: 1.5, 2.0)</small>
                            </div>
                        </div>
                    </div>

                    <hr />

                    @* Buttons *@
                    <div class="d-flex justify-content-between align-items-center">
                        <a href="@Url.Action("Index")" class="btn btn-secondary btn-icon-split">
                            <span class="icon text-white-50">
                                <i class="fas fa-arrow-left"></i>
                            </span>
                            <span class="text">Hủy bỏ</span>
                        </a>

                        <button type="submit" class="btn btn-success btn-icon-split shadow-sm">
                            <span class="icon text-white-50">
                                <i class="fas fa-check"></i>
                            </span>
                            <span class="text font-weight-bold">Thêm mới</span>
                        </button>
                    </div>
                }
            </div>
        </div>

        @* Hướng dẫn *@
        <div class="card shadow mb-4 border-left-info py-2">
            <div class="card-body">
                <div class="row no-gutters align-items-center">
                    <div class="col mr-2">
                        <div class="text-xs font-weight-bold text-info text-uppercase mb-1">
                            Hướng dẫn
                        </div>
                        <div class="h5 mb-0 font-weight-bold text-gray-800 text-sm">
                            <ul class="mb-0 pl-3">
                                <li>Ngày công: 1 = đủ công, 0.5 = nửa công, 0 = nghỉ</li>
                                <li>Giờ tăng ca: Nhập số giờ làm thêm ngoài giờ (nếu có)</li>
                                <li>Hệ thống sẽ tự động kiểm tra trùng ngày chấm công</li>
                            </ul>
                        </div>
                    </div>
                    <div class="col-auto">
                        <i class="fas fa-info-circle fa-2x text-gray-300"></i>
                    </div>
                </div>
            </div>
        </div>

    </div>
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}